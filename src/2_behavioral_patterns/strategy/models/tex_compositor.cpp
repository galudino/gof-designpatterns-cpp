#include "tex_compositor.h"

#include <iostream>

tex_compositor::tex_compositor() {

}

int tex_compositor::compose(float natural[], float stretch[], float shrink[],
                           int component_count, int line_width, int breaks[]) {

    return 0;
}
