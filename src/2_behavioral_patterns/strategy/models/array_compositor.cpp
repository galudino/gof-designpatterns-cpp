#include "array_compositor.h"

#include <iostream>

array_compositor::array_compositor(int interval) {

}

int array_compositor::compose(float natural[], float stretch[], float shrink[],
                             int component_count, int line_width, int breaks[]) {

    return 0;
}
